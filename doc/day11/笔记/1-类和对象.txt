开篇：
  用积木搭建城堡
    已经有一套现成积木

  造汽车

面向对象编程
  不关注对象内部的细节，关注的是如何使用对象来完成任务

方法：逐步建立面向对象编程的思维

1.编程思想概述
  面向过程的编程

    - 把完成某一个需求的 **所有步骤 从头到尾**逐步实现
    - 根据开发需求，将某些 **功能独立** 的代码 **封装** 成一个又一个 **函数（方法）**
    - 最后完成的代码，就是按逻辑调用 **不同的函数**

    > 在类中定义的函数，称为方法
    **特点**
    - 注重 **步骤与过程**，不注重职责分工
    - 如果需求复杂，代码会变得很复杂
    - **开发复杂项目，没有固定的套路，开发难度很大！**

    > 面向过程关注的是 **怎么做**

    **优点**：性能比面向对象高，因为类调用时需要实例化，开销比较大，比较消耗资源；比如单片机、嵌入式开发、Linux/Unix等一般采用面
    向过程开发，性能是最重要的因素。

    **缺点**：没有面向对象易维护、易复用、易扩展。

  面向对象的编程
     采用合适的方式方法让对象干活
     让应用开发变得更简单
     相比较函数，面向对象是更大的封装，根据职责在一个对象中封装多个方法
     - 在完成某一个需求前，首先确定职责 —— 要做的事情（方法）
     - 根据 职责确定不同的 对象，在对象内部封装不同的方法
     - 最后完成的代码，就是根据逻辑让 不同的对象调用 不同的方法来处理问题。

     特点
     - 注重 对象和职责，不同的对象承担不同的职责
     - 更加适合应对复杂的需求变化，是专门应对复杂项目开发，提供的固定套路
     - 需要在面向过程基础上，再学习一些面向对象的语法

2.类和对象

  印钞机（类 抽象）  人民币（对象 具体事物）
  通过类可以创建对象

  - 类是对一类具有相同 特征 或者 行为 的事物的一个统称，是抽象的，不能直接使用
    - 特征被称为 属性
    - 行为被称为 方法


  对象是一类事物的具体体现。对象是类的一个实例，必然具备该类事物的 属性 和 行为。

  - 对象是由类创建出来的一个具体存在，可以直接使用
  - 由哪一个类创建出来的对象，就拥有哪一个类中定义的属性和方法

  > 在程序开发中，应该先有类，再有对象


 - 类是模板，对象 是根据 类 这个模板创建出来的，应该 先有类，再有对象
 - 类只有一个，而对象可以有很多个
   - 不同的对象 之间属性可能会各不相同
   - 类中定义了什么属性和方法，对象中就有什么属性和方法，不可能多，也不可能少
   - 类 是对一类事物的描述，是 **抽象**的。
   - 对象是一类事物的实例，是 **具体**的。

 设计类 --> 创建对象 ---> 调用对象干活
                           访问对象的属性(成员变量)
                           调用对象的方法(成员方法)

  属性(特征)
  方法(行为)

  人 --->  张三
  姓名      张三
  性别      男

  吃饭
  睡觉

3.类和对象的使用

  class 类名 {
    // 成员变量
    // 成员方法
  }

  创建对象
  类名 对象名称(变量名称) = new 类名();

  对象名称.成员变量;
  对象名称.成员方法();

4.对象的内存图


5.类和对象练习

  步骤一：设计类
           类中有需要定义哪些成员变量
           类中应该设计哪些成员方法
  步骤二：  创建对象

  步骤三：调用对象干活

         访问对象的属性(成员变量)
         调用对象的方法(成员方法)

  注意：一个 java 文件中可以定义多个类，但是只有一个被 public 修饰

6.如何抽象出类
  热搜：
    1.新闻标题 title
    2.新闻链接 url
    3.新闻排序 sort
    4.是否显示热点tag  hotTag





























