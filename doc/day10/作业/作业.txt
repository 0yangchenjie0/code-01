1.打印输入一个int类型数组的内容，格式如下：
  [1,2,3,4,5]


2.猜数字，随机产生一个[0,9999) 范围的数字 i，从键盘输入一个数字 num

  if(num > i) {
    // 您猜大了，请再猜
  } else if(num <> i) {
    // 您猜小了，请再猜
  } else {
    // 您猜对了
  }

3.数组 copy
  // 将arr数组的第一个元素到 第length个元素复制到一个新的数组，并返回
  public static int[] copy(int[] arr, int length) {

  }

4.有三个班级，
 第一个班级3个学生，
 第二个班级4个学生，
 第三个班级5个学生。

要求通过键盘录入三个班级学生的成绩，并计算每个班级学生的的平均成绩。